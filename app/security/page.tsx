import {Navbar} from "@/components/marketing/navbar";import {Footer} from "@/components/marketing/footer";import {ShieldCheck,KeyRound,Database,UserCheck} from "lucide-react";
const items=[["Session security","HttpOnly, SameSite, signed session cookies with production Secure flags.",KeyRound],["Password storage","Node scrypt hashing with per-password random salts; raw passwords are never persisted.",ShieldCheck],["Workspace isolation","All authenticated application reads and mutations are scoped to the session workspace.",Database],["Approval authorization","Only owner, admin, and operator roles can mutate policy-gated approvals.",UserCheck]] as const;
export default function Page(){return <><Navbar/><main className="bg-white py-24"><div className="pc-shell"><p className="pc-kicker">Security</p><h1 className="mt-4 max-w-4xl text-5xl font-bold tracking-[-.05em]">Governed by default.</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-[#66706A]">The SaaS scaffold separates identity, workspace authorization, Copilot analysis, approval, and external execution boundaries.</p><div className="mt-10 grid gap-4 md:grid-cols-2">{items.map(([t,d,I])=><div key={t} className="pc-card p-6"><I className="size-6 text-[#1E6B4B]"/><h2 className="mt-5 font-bold">{t}</h2><p className="mt-2 text-sm leading-6 text-[#66706A]">{d}</p></div>)}</div></div></main><Footer/></>}
