# API generator integration

The runtime generator entrypoint lives in `api/api-generator/generate.mjs`. This directory documents API-generation conventions and keeps generator-specific source isolated from application runtime code.
