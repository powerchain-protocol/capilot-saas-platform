{
  "name": "@powerchain/copilot-sdk",
  "version": "1.0.0",
  "private": true,
  "description": "Typed first-party TypeScript client for PowerChain Copilot API v1.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --max-warnings=0",
    "check": "pnpm typecheck && pnpm lint",
    "clean": "node -e \"require('fs/promises').rm('dist',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "eslint": "10.9.1",
    "typescript": "7.0.2"
  }
}
