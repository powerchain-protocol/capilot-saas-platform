
# @powerchain/copilot-sdk

First-party typed TypeScript client for PowerChain Copilot API v1.

See `docs/SDK.md` for usage and authentication boundaries. The package intentionally does not invent bearer/API-key auth; v1 authenticated browser requests use the backend's signed HttpOnly cookie session.
