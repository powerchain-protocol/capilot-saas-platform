# API generator

`pnpm api:generate` reads the canonical OpenAPI JSON and writes the typed frontend operation catalog. `pnpm api:generate:check` fails when generated output is stale. The generator never emits credentials or API keys.
