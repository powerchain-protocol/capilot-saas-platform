project_id = "powerchain-copilot"

[api]
enabled = true
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 15

[db.pooler]
enabled = true
port = 54329
pool_mode = "transaction"
default_pool_size = 20
max_client_conn = 100

[realtime]
enabled = true

[studio]
enabled = true
port = 54323
api_url = "http://127.0.0.1"

[auth]
# PowerChain's existing server-issued session model remains authoritative.
# Enable Supabase Auth only after a deliberate auth migration.
enabled = false
site_url = "http://localhost:3000"
additional_redirect_urls = ["http://127.0.0.1:3000"]

[storage]
enabled = true
file_size_limit = "50MiB"
