import { Check } from "lucide-react";
import { pricingPlans } from "@/config/pricing";
import { Button } from "@/components/ui/button";
export function PricingSection({compact=false}:{compact?:boolean}){return <section className="bg-white py-24"><div className="pc-shell"><div className="mx-auto max-w-3xl text-center"><p className="pc-kicker">Pricing</p><h2 className="mt-4 text-4xl font-bold tracking-[-.045em] sm:text-5xl">Start small. Scale with operations.</h2><p className="mt-4 text-sm leading-7 text-[#66706A]">Pricing values are centralized so commercial terms can evolve without rewriting the interface.</p></div><div className="mt-12 grid gap-4 lg:grid-cols-3">{pricingPlans.map(plan=><article key={plan.id} className={`relative rounded-[22px] border p-6 ${plan.featured?"border-[#1E6B4B] bg-[#F4F9F6] shadow-[0_24px_70px_rgba(20,60,46,.12)]":"border-[#DCE2DD] bg-white"}`}>{plan.featured&&<span className="absolute right-5 top-5 rounded-full bg-[#143C2E] px-3 py-1 text-[9px] font-bold text-white">Most Popular</span>}<span className="text-[10px] font-bold uppercase tracking-[.12em] text-[#1E6B4B]">{plan.eyebrow}</span><h3 className="mt-4 text-2xl font-bold">{plan.name}</h3><p className="mt-3 min-h-12 text-sm leading-6 text-[#66706A]">{plan.description}</p><div className="mt-6"><b className="text-3xl tracking-[-.04em]">{plan.price}</b>{plan.suffix&&<span className="ml-1 text-xs text-[#78827C]">{plan.suffix}</span>}</div><Button href={plan.href} variant={plan.featured?"primary":"secondary"} className="mt-6 w-full">{plan.cta}</Button>{!compact&&<div className="mt-6 space-y-3 border-t border-[#E1E5E2] pt-6">{plan.features.map(f=><div key={f} className="flex items-center gap-2 text-xs"><Check className="size-4 text-[#167A4A]"/>{f}</div>)}</div>}</article>)}</div></div></section>}
